hardhat/
├── contracts/ # 📝 合约源代码（你写的 .sol 文件）
├── scripts/ # 🚀 部署和管理脚本（部署、交互等）
├── test/ # 🧪 测试文件（单元测试、集成测试）
├── artifacts/ # 🔨 编译产物（自动生成，包含 ABI 和 bytecode）
├── cache/ # ⚡ 编译缓存（加速编译，自动生成）
└── result/ # 📦 部署结果（你刚创建的，存放部署信息）
