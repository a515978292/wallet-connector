# 交易流程处理

> **学习时间**: Week 3-4, Day 8-10
> **学习目标**: 掌握完整的交易生命周期处理，提供良好的用户体验

---

## 一、交易生命周期

### 1.1 完整流程图

```
┌──────────────────────────────────────────────────────────┐
│                    交易完整流程                            │
└──────────────────────────────────────────────────────────┘

1. 用户触发操作
   ↓
2. 前端调用 writeContract()
   ↓ [状态: isPending = true]
3. MetaMask 弹窗 → 用户确认/拒绝
   │
   ├─→ 用户拒绝 → [状态: error]
   │              显示: "Transaction rejected"
   │
   └─→ 用户确认
       ↓
4. 交易发送到内存池（Mempool）
   ↓ [返回: transaction hash]
5. 等待矿工打包
   ↓ [状态: isConfirming = true]
   ↓ 大约 12-15 秒（以太坊）
6. 交易被打包到区块
   ↓
7. 交易确认
   │
   ├─→ 成功 → [状态: isConfirmed = true]
   │          刷新数据，显示成功提示
   │
   └─→ 失败 → [状态: error]
              显示错误原因（Gas 不足、Revert 等）
```

### 1.2 关键状态

| 阶段 | 状态标识 | 用户看到 | 持续时间 |
|------|----------|----------|----------|
| **等待签名** | `isPending = true` | "Waiting for signature..." | 取决于用户 |
| **签名被拒** | `error` (rejected) | "Transaction rejected" | - |
| **交易发送** | `hash` 返回 | "Transaction submitted" | 立即 |
| **等待确认** | `isConfirming = true` | "Confirming..." | ~15秒 |
| **确认成功** | `isConfirmed = true` | "Success!" | - |
| **确认失败** | `error` (reverted) | "Transaction failed" | - |

---

## 二、基础交易处理

### 2.1 最简单的实现

```typescript
import { useWriteContract, useWaitForTransactionReceipt } from 'wagmi'

function SimpleTransfer() {
  const { writeContract, data: hash, isPending } = useWriteContract()
  const { isLoading: isConfirming, isSuccess } = useWaitForTransactionReceipt({ hash })

  const handleTransfer = () => {
    writeContract({
      address: '0x...',
      abi: contractABI,
      functionName: 'transfer',
      args: ['0xRecipient...', 1000000n]
    })
  }

  return (
    <div>
      <button onClick={handleTransfer} disabled={isPending || isConfirming}>
        {isPending && '⏳ Waiting for signature...'}
        {isConfirming && '⏳ Confirming transaction...'}
        {!isPending && !isConfirming && 'Transfer'}
      </button>
      {isSuccess && <p>✅ Transaction successful!</p>}
    </div>
  )
}
```

---

## 三、完整的交易状态管理

### 3.1 使用 Toast 通知

```typescript
import { useWriteContract, useWaitForTransactionReceipt } from 'wagmi'
import { useEffect } from 'react'
import { toast } from 'sonner'  // 或 react-hot-toast

function TransferWithToast() {
  const {
    writeContract,
    data: hash,
    isPending,
    error: writeError
  } = useWriteContract()

  const {
    isLoading: isConfirming,
    isSuccess: isConfirmed,
    error: confirmError
  } = useWaitForTransactionReceipt({ hash })

  // 监听签名状态
  useEffect(() => {
    if (isPending) {
      toast.info('Please confirm the transaction in your wallet')
    }
  }, [isPending])

  // 监听交易提交
  useEffect(() => {
    if (hash) {
      toast.success('Transaction submitted!', {
        action: {
          label: 'View',
          onClick: () => window.open(`https://sepolia.etherscan.io/tx/${hash}`)
        }
      })
    }
  }, [hash])

  // 监听确认状态
  useEffect(() => {
    if (isConfirming) {
      toast.loading('Confirming transaction...', { id: 'confirming' })
    }
  }, [isConfirming])

  // 监听确认成功
  useEffect(() => {
    if (isConfirmed) {
      toast.success('Transaction confirmed!', { id: 'confirming' })
    }
  }, [isConfirmed])

  // 监听错误
  useEffect(() => {
    if (writeError) {
      if (writeError.message.includes('User rejected')) {
        toast.error('You rejected the transaction')
      } else {
        toast.error('Transaction failed')
      }
    }
  }, [writeError])

  useEffect(() => {
    if (confirmError) {
      toast.error('Transaction reverted', { id: 'confirming' })
    }
  }, [confirmError])

  const handleTransfer = () => {
    writeContract({
      address: '0x...',
      abi: contractABI,
      functionName: 'transfer',
      args: ['0xRecipient...', 1000000n]
    })
  }

  return (
    <button onClick={handleTransfer} disabled={isPending || isConfirming}>
      Transfer
    </button>
  )
}
```

### 3.2 自定义 Hook 封装

```typescript
// hooks/useTransactionStatus.ts
import { useEffect } from 'react'
import { toast } from 'sonner'
import type { Hash } from 'viem'

interface UseTransactionStatusProps {
  hash?: Hash
  isPending: boolean
  isConfirming: boolean
  isConfirmed: boolean
  error?: Error | null
}

export function useTransactionStatus({
  hash,
  isPending,
  isConfirming,
  isConfirmed,
  error
}: UseTransactionStatusProps) {
  // 等待签名
  useEffect(() => {
    if (isPending) {
      toast.info('Please confirm in your wallet...', { id: 'tx' })
    }
  }, [isPending])

  // 交易已提交
  useEffect(() => {
    if (hash) {
      toast.success('Transaction submitted!', {
        id: 'tx',
        action: {
          label: 'View',
          onClick: () => window.open(`https://sepolia.etherscan.io/tx/${hash}`)
        }
      })
    }
  }, [hash])

  // 等待确认
  useEffect(() => {
    if (isConfirming) {
      toast.loading('Confirming transaction...', { id: 'tx' })
    }
  }, [isConfirming])

  // 确认成功
  useEffect(() => {
    if (isConfirmed) {
      toast.success('Transaction confirmed!', { id: 'tx' })
    }
  }, [isConfirmed])

  // 错误处理
  useEffect(() => {
    if (error) {
      const message = error.message.includes('User rejected')
        ? 'Transaction rejected'
        : 'Transaction failed'
      toast.error(message, { id: 'tx' })
    }
  }, [error])
}

// 使用示例
function MyComponent() {
  const { writeContract, data: hash, isPending, error } = useWriteContract()
  const { isLoading: isConfirming, isSuccess: isConfirmed } =
    useWaitForTransactionReceipt({ hash })

  // 一行代码搞定状态提示
  useTransactionStatus({
    hash,
    isPending,
    isConfirming,
    isConfirmed,
    error
  })

  return <button onClick={() => writeContract({...})}>Transfer</button>
}
```

---

## 四、错误处理

### 4.1 常见错误类型

```typescript
function parseTransactionError(error: Error): string {
  const message = error.message.toLowerCase()

  if (message.includes('user rejected') || message.includes('user denied')) {
    return 'You rejected the transaction'
  }

  if (message.includes('insufficient funds')) {
    return 'Insufficient balance to complete this transaction'
  }

  if (message.includes('gas required exceeds allowance')) {
    return 'Not enough gas to complete transaction'
  }

  if (message.includes('execution reverted')) {
    // 尝试提取 revert reason
    const reasonMatch = message.match(/reason="([^"]*)"/)
    if (reasonMatch) {
      return `Transaction failed: ${reasonMatch[1]}`
    }
    return 'Transaction reverted (check contract requirements)'
  }

  if (message.includes('nonce too low')) {
    return 'Transaction nonce error, please try again'
  }

  if (message.includes('replacement transaction underpriced')) {
    return 'Transaction already pending'
  }

  if (message.includes('network')) {
    return 'Network error, please check your connection'
  }

  return 'Transaction failed, please try again'
}

// 使用示例
function TransferWithErrorHandling() {
  const { writeContract, error } = useWriteContract()

  useEffect(() => {
    if (error) {
      const friendlyMessage = parseTransactionError(error)
      toast.error(friendlyMessage)
      console.error('Transaction error:', error)
    }
  }, [error])

  // ...
}
```

### 4.2 错误边界组件

```typescript
import { Component, ReactNode } from 'react'

interface Props {
  children: ReactNode
  fallback?: ReactNode
}

interface State {
  hasError: boolean
  error?: Error
}

export class TransactionErrorBoundary extends Component<Props, State> {
  constructor(props: Props) {
    super(props)
    this.state = { hasError: false }
  }

  static getDerivedStateFromError(error: Error): State {
    return { hasError: true, error }
  }

  componentDidCatch(error: Error, errorInfo: any) {
    console.error('Transaction error:', error, errorInfo)
  }

  render() {
    if (this.state.hasError) {
      return this.props.fallback || (
        <div className="error-container">
          <h3>Something went wrong</h3>
          <p>{this.state.error?.message}</p>
          <button onClick={() => this.setState({ hasError: false })}>
            Try Again
          </button>
        </div>
      )
    }

    return this.props.children
  }
}

// 使用
function App() {
  return (
    <TransactionErrorBoundary>
      <TransferComponent />
    </TransactionErrorBoundary>
  )
}
```

---

## 五、UI 状态优化

### 5.1 按钮状态

```typescript
function TransactionButton() {
  const { writeContract, isPending } = useWriteContract()
  const { isLoading: isConfirming } = useWaitForTransactionReceipt({ hash })

  const getButtonState = () => {
    if (isPending) {
      return {
        disabled: true,
        text: '⏳ Waiting for signature...',
        className: 'btn-pending'
      }
    }
    if (isConfirming) {
      return {
        disabled: true,
        text: '⏳ Confirming transaction...',
        className: 'btn-confirming'
      }
    }
    return {
      disabled: false,
      text: 'Transfer',
      className: 'btn-ready'
    }
  }

  const buttonState = getButtonState()

  return (
    <button
      onClick={() => writeContract({...})}
      disabled={buttonState.disabled}
      className={buttonState.className}
    >
      {buttonState.text}
    </button>
  )
}
```

### 5.2 加载动画

```typescript
import { Loader2 } from 'lucide-react'  // 图标库

function TransactionButton() {
  const { writeContract, isPending } = useWriteContract()
  const { isLoading: isConfirming } = useWaitForTransactionReceipt({ hash })

  const isLoading = isPending || isConfirming

  return (
    <button
      onClick={() => writeContract({...})}
      disabled={isLoading}
      className="btn"
    >
      {isLoading && <Loader2 className="animate-spin mr-2" />}
      {isPending && 'Waiting for signature...'}
      {isConfirming && 'Confirming...'}
      {!isLoading && 'Transfer'}
    </button>
  )
}
```

### 5.3 交易历史面板

```typescript
interface Transaction {
  hash: string
  status: 'pending' | 'confirmed' | 'failed'
  timestamp: number
  description: string
}

function TransactionHistory() {
  const [transactions, setTransactions] = useState<Transaction[]>([])

  const addTransaction = (tx: Transaction) => {
    setTransactions(prev => [tx, ...prev])
  }

  return (
    <div className="transaction-history">
      <h3>Recent Transactions</h3>
      {transactions.map(tx => (
        <div key={tx.hash} className="transaction-item">
          <div className="flex items-center gap-2">
            {tx.status === 'pending' && <Loader2 className="animate-spin" />}
            {tx.status === 'confirmed' && <CheckCircle className="text-green-500" />}
            {tx.status === 'failed' && <XCircle className="text-red-500" />}
            <span>{tx.description}</span>
          </div>
          <a
            href={`https://sepolia.etherscan.io/tx/${tx.hash}`}
            target="_blank"
            className="text-blue-500"
          >
            View →
          </a>
        </div>
      ))}
    </div>
  )
}
```

---

## 六、自动刷新数据

### 6.1 交易成功后刷新余额

```typescript
import { useReadContract, useWriteContract, useWaitForTransactionReceipt } from 'wagmi'
import { useQueryClient } from '@tanstack/react-query'

function TransferWithRefresh() {
  const queryClient = useQueryClient()

  const { data: balance } = useReadContract({
    address: '0x...',
    abi: contractABI,
    functionName: 'balanceOf',
    args: [userAddress]
  })

  const { writeContract, data: hash } = useWriteContract()

  const { isSuccess } = useWaitForTransactionReceipt({
    hash,
    onSuccess() {
      // 交易成功后，手动刷新余额
      queryClient.invalidateQueries({
        queryKey: ['balance', userAddress]
      })

      toast.success('Balance updated!')
    }
  })

  return (
    <div>
      <p>Balance: {balance?.toString()}</p>
      <button onClick={() => writeContract({...})}>
        Transfer
      </button>
    </div>
  )
}
```

### 6.2 使用事件自动刷新

```typescript
import { useWatchContractEvent } from 'wagmi'

function AutoRefreshBalance() {
  const { address } = useAccount()
  const queryClient = useQueryClient()

  const { data: balance } = useReadContract({
    address: '0x...',
    abi: contractABI,
    functionName: 'balanceOf',
    args: [address!]
  })

  // 监听 Transfer 事件，自动刷新余额
  useWatchContractEvent({
    address: '0x...',
    abi: contractABI,
    eventName: 'Transfer',
    args: {
      to: address  // 只监听转账给当前用户的事件
    },
    onLogs() {
      queryClient.invalidateQueries({
        queryKey: ['balance', address]
      })
    }
  })

  return <div>Balance: {balance?.toString()}</div>
}
```

---

## 七、Gas 估算

### 7.1 使用 useSimulateContract

```typescript
import { useSimulateContract, useWriteContract } from 'wagmi'
import { formatGwei } from 'viem'

function TransferWithGasEstimate() {
  const [toAddress, setToAddress] = useState('')
  const [amount, setAmount] = useState('')

  // 模拟交易，获取 Gas 估算
  const { data: simulateData, error: simulateError } = useSimulateContract({
    address: '0x...',
    abi: contractABI,
    functionName: 'transfer',
    args: [toAddress as `0x${string}`, BigInt(amount || 0)],
    query: {
      enabled: !!toAddress && !!amount  // 只有填写完才模拟
    }
  })

  const { writeContract } = useWriteContract()

  const estimatedGas = simulateData?.request?.gas
  const gasPrice = simulateData?.request?.gasPrice

  return (
    <div>
      <input value={toAddress} onChange={e => setToAddress(e.target.value)} />
      <input value={amount} onChange={e => setAmount(e.target.value)} />

      {estimatedGas && (
        <div className="gas-estimate">
          <p>Estimated Gas: {estimatedGas.toString()}</p>
          {gasPrice && (
            <p>Gas Price: {formatGwei(gasPrice)} Gwei</p>
          )}
        </div>
      )}

      {simulateError && (
        <p className="error">
          Transaction will fail: {simulateError.message}
        </p>
      )}

      <button
        onClick={() => writeContract(simulateData!.request)}
        disabled={!simulateData || !!simulateError}
      >
        Transfer
      </button>
    </div>
  )
}
```

---

## 八、交易流程最佳实践

### 8.1 完整示例：Todo DApp

```typescript
import { useState, useEffect } from 'react'
import {
  useWriteContract,
  useWaitForTransactionReceipt,
  useReadContract,
  useWatchContractEvent
} from 'wagmi'
import { toast } from 'sonner'

function TodoApp() {
  const [newTodo, setNewTodo] = useState('')
  const [todos, setTodos] = useState<string[]>([])

  // 读取现有 todos
  const { data: todosData, refetch } = useReadContract({
    address: '0x...',
    abi: contractABI,
    functionName: 'getTodos'
  })

  // 添加 todo
  const {
    writeContract,
    data: hash,
    isPending,
    error: writeError
  } = useWriteContract()

  // 等待确认
  const {
    isLoading: isConfirming,
    isSuccess
  } = useWaitForTransactionReceipt({ hash })

  // 监听 TodoAdded 事件
  useWatchContractEvent({
    address: '0x...',
    abi: contractABI,
    eventName: 'TodoAdded',
    onLogs() {
      refetch()  // 自动刷新列表
    }
  })

  // 更新本地状态
  useEffect(() => {
    if (todosData) {
      setTodos(todosData as string[])
    }
  }, [todosData])

  // 状态提示
  useEffect(() => {
    if (isPending) toast.info('Confirm in wallet...')
  }, [isPending])

  useEffect(() => {
    if (isConfirming) toast.loading('Adding todo...', { id: 'tx' })
  }, [isConfirming])

  useEffect(() => {
    if (isSuccess) {
      toast.success('Todo added!', { id: 'tx' })
      setNewTodo('')  // 清空输入框
    }
  }, [isSuccess])

  useEffect(() => {
    if (writeError) {
      toast.error('Failed to add todo')
    }
  }, [writeError])

  // 添加 todo
  const handleAdd = () => {
    if (!newTodo.trim()) return

    writeContract({
      address: '0x...',
      abi: contractABI,
      functionName: 'addTodo',
      args: [newTodo]
    })
  }

  return (
    <div>
      <h1>My Todo List</h1>

      {/* 添加表单 */}
      <div className="flex gap-2 mb-4">
        <input
          type="text"
          value={newTodo}
          onChange={e => setNewTodo(e.target.value)}
          placeholder="Enter todo..."
          disabled={isPending || isConfirming}
        />
        <button
          onClick={handleAdd}
          disabled={isPending || isConfirming || !newTodo.trim()}
        >
          {isPending && '⏳ Signing...'}
          {isConfirming && '⏳ Adding...'}
          {!isPending && !isConfirming && 'Add Todo'}
        </button>
      </div>

      {/* 交易 hash */}
      {hash && (
        <a
          href={`https://sepolia.etherscan.io/tx/${hash}`}
          target="_blank"
          className="text-blue-500"
        >
          View transaction →
        </a>
      )}

      {/* Todo 列表 */}
      <ul>
        {todos.map((todo, i) => (
          <li key={i}>{todo}</li>
        ))}
      </ul>
    </div>
  )
}
```

---

## 九、调试技巧

### 9.1 查看完整错误信息

```typescript
function DebugTransaction() {
  const { writeContract, error } = useWriteContract()

  useEffect(() => {
    if (error) {
      console.group('Transaction Error Details')
      console.log('Message:', error.message)
      console.log('Name:', error.name)
      console.log('Cause:', error.cause)
      console.log('Full Error:', error)
      console.groupEnd()
    }
  }, [error])

  // ...
}
```

### 9.2 模拟交易调试

```typescript
import { useSimulateContract } from 'wagmi'

function SimulateDebug() {
  const { data, error } = useSimulateContract({
    address: '0x...',
    abi: contractABI,
    functionName: 'transfer',
    args: ['0xRecipient...', 1000000n]
  })

  console.log('Simulate Result:', data)
  console.log('Simulate Error:', error)

  // 如果模拟成功，再执行实际交易
  return <button onClick={() => writeContract(data!.request)}>Transfer</button>
}
```

---

## 十、学习检查点

完成本节学习后，你应该能够：

- [ ] 理解完整的交易生命周期
- [ ] 处理所有交易状态（pending、confirming、success、error）
- [ ] 实现友好的用户提示（Toast、Loading）
- [ ] 解析和展示常见错误
- [ ] 交易成功后自动刷新数据
- [ ] 监听合约事件并更新 UI
- [ ] 估算 Gas 费用
- [ ] 调试交易失败原因

---

## 十一、下一步

完成 Day 8-10 的学习后，进入：
- 📁 [Day 11-14](../day11-14/) - 合约交互实战（Todo List DApp）

---

**更新时间**: 2024-12-05
**作者**: AI 助手
